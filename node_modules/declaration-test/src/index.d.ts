import { One } from 'declaration-test/src/One'
import { Two } from 'declaration-test/src/Two'
import { Three } from 'declaration-test/src/Three'
import { Combo } from 'declaration-test/src/Combo'

export declare module DeclarationTest {
    export class DeclarationTest {
        constructor ();
        one (two: Two, three: Three) : string;
        two (one: One, three: Three) : number;
        three (one: One, two: Two) : string;
        combo (combo: Combo) : string;
    }
}
